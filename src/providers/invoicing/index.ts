/**
 * Invoicing provider auto-registration.
 *
 * Import this module anywhere that needs the registry populated (API handlers,
 * pages, dispatcher). Importing it triggers each provider module's
 * `registerProvider()` call.
 */
import './fgo.ts';
